pop up a contextual menu to change properties and edit the script for the control under the mouse pointer, the current card, and the current stack.

To use this shortcut, the Pointer tool must be in use, and the stack you click in must be in an editable window (not a palette or dialog box).